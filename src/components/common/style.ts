import {responseDash as rd} from '~/components/common/ResponsiveSize';

export const DASH_WIDTH: number = rd(0.4);

export const ITEM_BORDER_WIDTH: number = rd(0.21);

export const FONT_NAME = 'omyu pretty';
