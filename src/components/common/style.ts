import {
  heightSizePercentage as hp,
  widthSizePercentage as wp,
  responseFont as rf,
} from '~/components/common/ResponsiveSize';

export const DASH_WIDTH: number = wp(0.4);

export const ITEM_BORDER_WIDTH: number = wp(0.21);

export const FONT_NAME = 'omyu pretty';

export const BUTTON_PADDING: number = hp(1.6);

export const BUTTON_RADIUS: number = wp(2);

export const BUTTON_FONT_SIZE: number = rf(16);

export const AREA_FONT_SIZE: number = rf(17);

export const BACK_FONT_SIZE: number = rf(17.8);

export const HEADER_FONT_SIZE: number = rf(20.5);
